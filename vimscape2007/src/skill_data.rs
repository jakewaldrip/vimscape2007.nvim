pub struct SkillData {
    pub skill_name: String,
    pub total_exp: i32,
    pub level: i32,
}

impl SkillData {
    // TODO: Format each line here
    pub fn format_skill_data(&self) -> String {
        "heha".into()
    }
}
